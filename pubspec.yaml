name: form_map
description: "form_map is a Flutter package that simplifies handling form data using enums, making your code more organized and readable."
version: 0.1.0
repository: https://github.com/elnaddar/form_map
license: MIT

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

platforms:
  android:
  ios:
  web:
  linux:
  macos:
  windows:
